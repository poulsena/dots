# Auto start programs
autostart="xcompmgr numlockx screensettings defaultapplications remaps"

for program in $autostart; do
	pidof -sx "$program" || "$program" &
done >/dev/null 2>&1

. ~/.config/shell/startupscripts/startdwm
